export type RecordMode = 'none' | 'single' | 'loop-single' | 'loop-auto';
export type RecordingPhase = 'idle' | 'warmup' | 'recording' | 'auto';

export const MAX_SCRATCHES = 36;
export const MAX_LOOP_TAKES = 12;
